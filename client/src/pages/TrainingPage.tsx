import { PlaceholderPage } from '../features/placeholder/PlaceholderPage';

// Pestaña Entreno (autorregulación por rol) — placeholder hasta su fase.
export function TrainingPage() {
  return <PlaceholderPage tab="Entreno" />;
}

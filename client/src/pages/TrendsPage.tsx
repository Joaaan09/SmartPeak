import { PlaceholderPage } from '../features/placeholder/PlaceholderPage';

// Pestaña Tendencias (históricos HRV/sueño/peso) — placeholder hasta su fase.
export function TrendsPage() {
  return <PlaceholderPage tab="Tendencias" />;
}
